{% extends "base.twig" %}

{% block outside_content %}
	<div class="content__archis">
{# 		<div class="archis__subpage">
 #}			<div class="subpage__title">
				<h3>{{post.title}}</h3>
			</div>

			<div class="page__content">
				{{ post.content }}
			</div>
{# 		</div>
 #}	</div>
{% endblock %}